import App from './App.js'

/**
 * 导出flux对象
 */
export default {
    /**
     * 创建App
     * @return {App}
     */
    createApp(name, config={}, env='dev', id=''){

    },

    version: '0.0.1'
}